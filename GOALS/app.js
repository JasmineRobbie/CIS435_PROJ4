function goalTable(){
    var table = document.createElement("table");
    var GOALS = document.getElementById("goal").value + "</br>";
    var task = document.getElementById("goal").value + "</br>";
    var difficulty = document.getElementById("difficulty").value + "</br>";
    for(var i = 1; i <= task; i++)
    {
        var row = table.insertRow(-1)
        for (var i = 1; i <= )
    }
    table.appendChild(rows);
    table.appendChild(cols);
} 


<script src="app.js"/>